<h1>篇章：<%= @note.title %></h1>
<%= @note.content %>

<%= form_with(model: @comment, url: note_comments_path(@note)) do |f| %>
  <%= f.text_area :content %>
  <%= f.submit "新增留言" %>
<% end %>

<ul>
  <% @comments.each do |c| %>
  <li>
    <%= c.content %>
  </li>
  <% end %>
</ul>
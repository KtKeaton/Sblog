<h1>筆記列表</h1>

<%= link_to "新增筆記", new_note_path %>

<table>
  <tr>
    <td>Title</td>

  <% @notes.each do |note| %>
  <tr>
    <td>
      <%= link_to note.title, note_path(note) %>
    </td>
  </tr>
  <% end %>
</table>
<h1>筆記列表</h1>

<%= link_to "新增筆記", new_note_path %>

<table>
  <tr>
    <td>Title</td>
    <td>Content</td>

    <% @notes.each do |note| %>
  <tr>
    <td>
      <%= note.title %>
    </td>
    <td>
      <%= note.content %>
    </td>
  </tr>
  <% end %>
</table>